# Отчёт: Команда `/help`
16 марта 2026 г.  


---

##  Что было сделано

### 1. **Добавлена команда `/help`**

**Файл:** `bot/handlers/common.py`

**Описание:**
- Обработчик команды `/help` с подробной справкой о боте
- Информация о возможностях бота
- Список всех доступных команд
- Предупреждение о том, что бот не заменяет профессионального психолога

**Код:**
```python
@router.message(Command("help"))
async def cmd_help(message: types.Message):
    help_text = (
        "**Твой психолог — справка**\n\n"
        "Я бот для психологической поддержки и помощи в стрессовых ситуациях.\n\n"
        "**Что я умею:**\n"
        "• Выслушаю и поддержу в трудную минуту\n"
        "• Помогу разобраться в эмоциях\n"
        "• Предложу техники самопомощи\n"
        "• Порекомендую книги и упражнения\n\n"
        "**Команды:**\n"
        "/start — начать новый диалог\n"
        "/help — эта справка\n"
        "/info — телефоны служб помощи\n\n"
        "**Важно:**\n"
        "Я не заменяю профессионального психолога. "
        "Если тебе нужна срочная помощь, используй команду /info"
    )
    await message.reply(
        help_text,
        parse_mode="Markdown",
        reply_markup=get_main_keyboard()
    )
```

---

### 2. **Обновлена команда `/start`**

**Файл:** `bot/handlers/common.py`

**Изменения:**
- Добавлена информация о доступных командах в приветственное сообщение

**Код:**
```python
@router.message(Command("start"))
async def cmd_start(message: types.Message):
    user_id = message.from_user.id
    dialog_history.clear_history(user_id)
    await message.reply(
        "Привет! Я бот психологической поддержки. Напиши мне, что тебя беспокоит.\n\n"
        "Доступные команды:\n"
        "/help — справка о боте\n"
        "/info — службы помощи\n"
        "/start — начать новый диалог",
        reply_markup=get_main_keyboard()
    )
```

---

### 3. **Обновлен список команд бота**

**Файл:** `bot/main.py`

**Изменения:**
- Добавлена команда `/help` в меню команд Telegram

**Код:**
```python
async def set_bot_commands(bot: Bot):
    commands = [
        BotCommand(command="start", description="Начать новый диалог"),
        BotCommand(command="help", description="Справка о боте"),
        BotCommand(command="info", description="Службы помощи"),
    ]
    await bot.set_my_commands(commands)
```

---

##  Результат

### **Вид в Telegram:**

**Команда `/help`:**
```
 Твой психолог — справка

Я бот для психологической поддержки и помощи в стрессовых ситуациях.

Что я умею:
• Выслушаю и поддержу в трудную минуту
• Помогу разобраться в эмоциях
• Предложу техники самопомощи
• Порекомендую книги и упражнения

Команды:
/start — начать новый диалог
/help — эта справка
/info — телефоны служб помощи

Важно:
Я не заменяю профессионального психолога. 
Если тебе нужна срочная помощь, используй команду /info
```

**Меню команд (кнопка `/` в поле ввода):**
| Команда | Описание |
|---------|----------|
| `/start` | Начать новый диалог |
| `/help` | Справка о боте |
| `/info` | Службы помощи |

---

## Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `bot/handlers/common.py` | +30 строк (функция `cmd_help`, обновлён `cmd_start`) |
| `bot/main.py` | +1 команда в `set_bot_commands()` |

---

## Команды для проверки

```bash
# Запуск бота (локально)
python -m bot.main

# Проверка в Telegram:
# 1. Отправить /start — увидеть список команд
# 2. Отправить /help — получить справку
# 3. Нажать кнопку меню — увидеть /help в списке
```

